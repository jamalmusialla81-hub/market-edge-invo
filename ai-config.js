window.MARKET_EDGE_AI_CONFIG = Object.freeze({
  // Set only to the deployed Cloudflare Worker origin, never to an OpenAI API key.
  apiBase: 'https://market-edge-ai.jakob-market-edge.workers.dev',
  mode: 'free'
});
