#!/usr/bin/env node
// Re-labels only frozen V2 UNRESOLVED_DATA_GAP outcomes with a validated,
// explicitly labelled Binance USD-M proxy. It cannot create scans or rewrite
// candidate inputs.
import {mkdirSync,readFileSync,writeFileSync,existsSync} from 'node:fs';
import {join} from 'node:path';
import Recovery from './outcome-recovery.js';
import Rank from './historical-rank.js';
const API=(process.env.MARKET_EDGE_API||'https://market-edge-ai.jakob-market-edge.workers.dev').replace(/\/$/,''),TOKEN=process.env.MARKET_EDGE_RESEARCH_TOKEN||'',CF=process.env.CLOUDFLARE_API_TOKEN||'',ACCOUNT=process.env.CLOUDFLARE_ACCOUNT_ID||'8ea7796a8fb13ffb612245e8a08a55d6',DB=process.env.MARKET_EDGE_D1_DATABASE_ID||'39a4082e-41a4-45e9-9b76-99cf10eaca01',LIMIT=Math.max(1,Math.min(500,Number(process.env.OUTCOME_RECOVERY_LIMIT)||10)),CACHE=process.env.OUTCOME_CACHE_DIR||'research/outcome-cache',REPORT=process.env.OUTCOME_RECOVERY_REPORT||'outcome-recovery-report.json';
const hash=value=>Rank.hash(value),parse=value=>{try{return JSON.parse(value||'{}');}catch{return{};}};
async function d1(sql,params=[]){if(!CF)throw new Error('CLOUDFLARE_API_TOKEN is required');const response=await fetch(`https://api.cloudflare.com/client/v4/accounts/${ACCOUNT}/d1/database/${DB}/query`,{method:'POST',headers:{authorization:`Bearer ${CF}`,'content-type':'application/json'},body:JSON.stringify({sql,params})}),body=await response.json().catch(()=>({}));if(!response.ok||body.success===false)throw new Error(`D1 query failed: ${body?.errors?.[0]?.message||response.status}`);return body.result?.[0]?.results||[];}
async function ingest(payload){if(!TOKEN)throw new Error('MARKET_EDGE_RESEARCH_TOKEN is required');const response=await fetch(`${API}/v1/research/ingest`,{method:'POST',headers:{authorization:`Bearer ${TOKEN}`,'content-type':'application/json'},body:JSON.stringify(payload)}),body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(`Recovery ingest failed: ${body?.error?.code||response.status} ${body?.error?.message||''}`);return body;}
function cachePath(symbol,start,end){return join(CACHE,`${symbol}-${start}-${end}-binance-usdm-5m.json`);}
async function candles(asset,start){const symbol=Recovery.symbolFor(asset);if(!symbol)return[];const end=start+Rank.OUTCOME_BARS*Rank.BASE_MS,path=cachePath(symbol,start,end);if(existsSync(path))return Recovery.candlesFromBinance(JSON.parse(readFileSync(path,'utf8')).rows);const url=`https://fapi.binance.com/fapi/v1/klines?symbol=${symbol}&interval=5m&startTime=${start}&endTime=${end-1}&limit=${Rank.OUTCOME_BARS}`;const response=await fetch(url),payload=await response.json();if(!response.ok||!Array.isArray(payload))throw new Error(`Binance proxy request failed for ${symbol}: ${response.status}`);mkdirSync(CACHE,{recursive:true});writeFileSync(path,JSON.stringify({source:'BINANCE_PROXY',symbol,start,end,rows:payload})+'\n');return Recovery.candlesFromBinance(payload);}
async function run(){const rows=await d1(`SELECT c.candidate_id,c.scan_id,c.asset,c.direction,c.strategy,c.stop,c.rr,s.scan_timestamp,seq.sequence_json FROM historical_scan_candidates c JOIN historical_scan_snapshots s ON s.scan_id=c.scan_id LEFT JOIN historical_candidate_sequences seq ON seq.candidate_id=c.candidate_id WHERE c.valid_current_geometry=1 AND c.targets_json LIKE '%UNRESOLVED_DATA_GAP%' AND s.engine_version='HISTORICAL-RANK-PILOT-V2' ORDER BY s.scan_timestamp,c.candidate_id LIMIT ?`,[LIMIT]);const groups=new Map(),report={attempted:rows.length,resolved:0,still_unresolved:0,source:'BINANCE_PROXY',no_lookahead:true,results:[]};for(const row of rows){const sequence=parse(row.sequence_json),safe=Object.values(sequence.timeframes||{}).every(frame=>Number(frame.window_end||0)<=Number(row.scan_timestamp));if(!safe)throw new Error(`LOOKAHEAD_REJECTED: ${row.candidate_id}`);const candidate={id:row.candidate_id,asset:row.asset,direction:row.direction,strategy:row.strategy,stop:Number(row.stop),rr:Number(row.rr),timestamp:Number(row.scan_timestamp),valid_current_geometry:true},target=Recovery.resolveProxy(candidate,await candles(row.asset,candidate.timestamp));report.no_lookahead&&=safe;report.results.push({candidate_id:row.candidate_id,scan_id:row.scan_id,status:target.status,source:target.outcome_source||null});if(target.status==='RESOLVED'){report.resolved++;const list=groups.get(row.scan_id)||[];list.push({candidate_id:row.candidate_id,targets:target,outcome_hash:hash({candidate_id:row.candidate_id,targets:target})});groups.set(row.scan_id,list);}else report.still_unresolved++;}
for(const [scan_id,outcomes] of groups)await ingest({operation:'historical_rank_outcome_recovery_commit',scan_id,outcomes});report.threshold_pass=report.resolved>=Math.min(8,report.attempted);writeFileSync(REPORT,JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report));if(rows.length&& !report.threshold_pass)process.exitCode=2;}
run().catch(error=>{console.error(`outcome recovery failed: ${error.message}`);process.exitCode=1;});
